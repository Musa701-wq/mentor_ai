import 'package:flutter/material.dart';

// Global RouteObserver to allow screens to react when returning from routes
final RouteObserver<PageRoute<dynamic>> routeObserver = RouteObserver<PageRoute<dynamic>>();